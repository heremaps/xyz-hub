{
  "id": "storage-for-hiding-password-test-0022",
  "type": "Storage",
  "title": "Test PSQL storage",
  "description": "PSQL storage instance for testing purpose",
  "className": "com.here.naksha.lib.psql.PsqlStorage",
  "properties": {
    "master": {
      "host": "${dataDb.host}",
      "db": "${dataDb.db}",
      "user": "${dataDb.user}",
      "password": "${dataDb.password}",
      "readOnly": false
    },
    "reader": [
      {
        "host": "${dataDb.host}",
        "db": "${dataDb.db}",
        "user": "${dataDb.user}",
        "password": "${dataDb.password}",
        "readOnly": true
      }
    ],
    "appName": "test-app-name",
    "schema": "${dataDb.schema}"
  }
}